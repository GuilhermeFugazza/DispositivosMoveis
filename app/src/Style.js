import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    fontG:{
        fontSize: 24,
        color: '#000',
    },

    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },

    Button: {
       // width: 300,
       color: 'red',
    }
})